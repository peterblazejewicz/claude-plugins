# Port ledger

Per-skill port status against upstream [addyosmani/agent-skills](https://github.com/addyosmani/agent-skills) at the pinned commit recorded in [`UPSTREAM.md`](./UPSTREAM.md).

**Status values**

- `pending` — not ported yet; upstream file exists only in `vendor/`
- `as-is` — ported with frontmatter/footer only; body unchanged
- `modified` — ported with targeted .NET/C# adaptations; upstream body largely preserved
- `rewritten` — structurally incompatible; re-written for .NET; upstream diff tracking stopped
- `skipped` — intentionally not ported (document why in the notes column)

## Skills

| Upstream | Wave | Ported as | Status | Last sync SHA7 | Notes |
|----------|------|-----------|--------|----------------|-------|
| skills/api-and-interface-design/SKILL.md | 1B | api-and-interface-design | modified | `44dac80` | C# records/interfaces with mandatory `CancellationToken`, ProblemDetails error semantics, FluentValidation at boundaries, DTO-vs-entity guidance, strongly-typed ID records, pattern-match discriminated unions |
| skills/browser-testing-with-devtools/SKILL.md | 2 | integration-testing-dotnet | rewritten | `44dac80` | Retargeted from Chrome DevTools MCP to four .NET integration boundaries: `WebApplicationFactory<Program>` (HTTP), Testcontainers (DB with real provider), `Microsoft.Playwright` (Blazor/Razor/MVC), `Avalonia.Headless.XUnit` (desktop UI); explicit ban on `EntityFrameworkCore.InMemory`; upstream "browser content is untrusted data" security guidance preserved and adapted to `page.EvaluateAsync`. **Downstream patch 2026-04-19 (skill v1.0.1):** HTTP-boundary csproj block annotated with xUnit v3 + Microsoft.Testing.Platform alternative. **Downstream patch 2026-04-19 (skill v1.0.2, plugin v2.3.0):** FluentAssertions removed; xUnit v3 + MTP promoted to the single canonical csproj setup (v2 relegated to a short "swap these lines" note); all HTTP / DB / Playwright / Avalonia test bodies rewritten to native `Xunit.Assert.X` (`Assert.Single`, `Assert.Equal`, `Assert.Empty`, `Assert.Contains`); added Avalonia xUnit version-cliff callout — `Avalonia.Headless.XUnit` only gained xUnit v3 support in Avalonia 12.0 (April 2026, AvaloniaUI/Avalonia#20481), so Avalonia 11.x projects must stay on xUnit v2 for headless UI tests. |
| skills/ci-cd-and-automation/SKILL.md | 2 | ci-cd-and-automation | modified | `44dac80` | GitHub Actions uses `setup-dotnet@v4` + `global.json`; quality gates become `dotnet format --verify-no-changes` / `dotnet build -warnaserror` / `dotnet test` / `dotnet list package --vulnerable`; EF Core migrations + Testcontainers; Playwright.NET E2E; Azure DevOps alternative; NuGet pack+push on tags; Azure App Service slot-per-PR preview deploys |
| skills/code-review-and-quality/SKILL.md | 1A | code-review-and-quality | modified | `44dac80` | Five-axis checks + DI lifetime, async correctness, EF Core N+1; `dotnet list package --vulnerable` replaces `npm audit`; `.editorconfig` as style authority |
| skills/code-simplification/SKILL.md | 1A | code-simplification | modified | `44dac80` | Added C# language-specific section (async unwrap, null-coalescing, LINQ, `switch` expression, expression-bodied, `record struct`); TS/Python/React sections preserved |
| skills/context-engineering/SKILL.md | 1A | context-engineering | modified | `44dac80` | CLAUDE.md template rewritten for .NET 8 / Avalonia / ASP.NET Core / EF Core stack; MCP table adds Microsoft Learn; conflict example uses Minimal APIs vs controllers |
| skills/debugging-and-error-recovery/SKILL.md | 1A | debugging-and-error-recovery | modified | `44dac80` | Triage/reproduce/bisect use `dotnet test --filter`; runtime table covers DbContext concurrency, missing DI, `TaskCanceledException`; safe-fallback examples in C# with `OperationCanceledException` filter |
| skills/deprecation-and-migration/SKILL.md | 1B | deprecation-and-migration | modified | `44dac80` | `[Obsolete]` as advisory/compulsory mechanism; adapter example in C# bridging sync→async; `IOptionsMonitor`-backed feature-flag migration; NuGet unlist and Roslyn analyzer code-fix migration tooling |
| skills/documentation-and-adrs/SKILL.md | 1B | documentation-and-adrs | modified | `44dac80` | ADR example uses EF Core + Npgsql with Dapper/SQLite/MongoDB alternatives; XML doc comments replace JSDoc; Minimal API + Swashbuckle OpenAPI metadata; README template uses dotnet CLI + `global.json` + `dotnet user-secrets` |
| skills/frontend-ui-engineering/SKILL.md | 3 | frontend-ui-engineering-avalonia | rewritten | `44dac80` | Retargeted from React/TS/Tailwind to Avalonia 11/12 with CommunityToolkit.Mvvm; compiled bindings (`x:DataType` + `x:CompileBindings`) as correctness tool; `ResourceDictionary.ThemeDictionaries` for Light/Dark variants; `AutomationProperties` for accessibility; class-based adaptive layouts across desktop/mobile/browser targets; cross-reference to companion avalonia-dev plugin for structural reviews |
| skills/git-workflow-and-versioning/SKILL.md | 1B | git-workflow-and-versioning | modified | `44dac80` | Pre-commit uses `dotnet test` / `dotnet format --verify-no-changes` / `dotnet build -warnaserror`; Husky.Net task runner replaces lint-staged; `.gitignore` includes `bin/`/`obj/`/`.vs/`; commit-message examples reference FluentValidation + WebApplicationFactory + Directory.Packages.props |
| skills/idea-refine/SKILL.md | 1B | idea-refine | modified | `44dac80` | Removed `scripts/idea-refine.sh` bootstrap; removed `AskUserQuestion` tool reference; codebase-awareness bullet references `MyApp.Core`/`MyApp.Infrastructure`, `Directory.Packages.props`, EF Core models; Phase 2 feasibility names .NET-specific axes |
| skills/idea-refine/examples.md | 1B | idea-refine/references/examples.md | as-is | `44dac80` | Tech-agnostic ideation examples (restaurants, doc editor, retros); content preserved verbatim |
| skills/idea-refine/frameworks.md | 1B | idea-refine/references/frameworks.md | as-is | `44dac80` | SCAMPER, HMW, First Principles, JTBD, constraint ideation, pre-mortem, analogous inspiration; content preserved verbatim |
| skills/idea-refine/refinement-criteria.md | 1B | idea-refine/references/refinement-criteria.md | as-is | `44dac80` | Evaluation rubric, assumption audit, decision matrix, MVP scoping principles; content preserved verbatim |
| skills/idea-refine/scripts/idea-refine.sh | 1B | — | skipped | `44dac80` | Bash bootstrap helper intentionally not ported — .NET teams rarely invoke bash skill bootstrappers; `docs/ideas/` treated as a path convention |
| skills/incremental-implementation/SKILL.md | 1A | incremental-implementation | modified | `44dac80` | `dotnet test` / `dotnet build -warnaserror` / `dotnet format` in increment checklist; feature flag example uses `IOptions<FeatureOptions>`; EF Core migration rollback guidance added |
| skills/performance-optimization/SKILL.md | 3 | performance-optimization-dotnet | rewritten | `44dac80` | Retargeted from Core Web Vitals / Lighthouse / React re-renders to BenchmarkDotNet (`[MemoryDiagnoser]`), dotnet-counters, dotnet-trace, PerfView, dotnet-gcdump; .NET performance targets (p95 latency, allocations/request, Gen2 rate, thread-pool queue, cold start, UI frame time); anti-patterns include EF Core N+1 / cartesian `Include`, sync-over-async, `Span<T>`/`ArrayPool<T>` discipline, `IHttpClientFactory`, `AddOutputCache`, `[GeneratedRegex]`, Kestrel tuning; 5-step MEASURE→IDENTIFY→FIX→VERIFY→GUARD workflow preserved |
| skills/planning-and-task-breakdown/SKILL.md | 1A | planning-and-task-breakdown | modified | `44dac80` | Task verification uses `dotnet test --filter` / `dotnet build -warnaserror`; file-path examples use `.cs`; dependency graph labels EF Core + Avalonia/Blazor/MAUI |
| skills/security-and-hardening/SKILL.md | 1B | security-and-hardening | modified (heavy) | `44dac80` | Full .NET rewrite: FluentValidation + EF Core parameterization + ASP.NET Core Identity/JWT bearer + policy-based authz + Data Protection + antiforgery + `dotnet user-secrets` / Key Vault + `dotnet list package --vulnerable` + `AddRateLimiter`; OWASP numbering + three-tier boundary frame preserved |
| skills/shipping-and-launch/SKILL.md | 1B | shipping-and-launch | modified | `44dac80` | Pre-launch checklist uses `dotnet` CLI and .NET security/perf/accessibility; `IOptions<FeatureOptions>` + `Microsoft.FeatureManagement` feature flags; monitoring via Application Insights / OpenTelemetry / dotnet-counters; EF Core migration rollback via `dotnet ef database update`; expand-contract schema pattern added |
| skills/source-driven-development/SKILL.md | 1B | source-driven-development | modified | `44dac80` | Stack detection reads `global.json`/`Directory.Packages.props`/`.csproj`; source hierarchy retargeted to Microsoft Learn + framework docs + official release notes; analyzer diagnostics (`CAxxxx`/`EFxxxx`) added as executable documentation; conflict/unverified examples rewritten for EF Core async vs sync and Npgsql JSONB |
| skills/spec-driven-development/SKILL.md | 0 | spec-driven-development | modified | `44dac80` | .NET framing: `dotnet` CLI commands, C# solution layout, EF Core/xUnit references replace npm/React/Prisma |
| skills/test-driven-development/SKILL.md | 2 | test-driven-development | modified | `44dac80` | Dual xUnit + MSTest samples with FluentAssertions; `TimeProvider` + `FakeTimeProvider` for time-dependent tests; test pyramid labels tie to `WebApplicationFactory`/Testcontainers/Playwright/Avalonia.Headless; explicit warning against `EntityFrameworkCore.InMemory`; upstream "Browser Testing with DevTools" section replaced with a pointer to `integration-testing-dotnet`. **Downstream patch 2026-04-19 (skill v1.0.1):** added "Version Awareness: xUnit v2 vs v3, and Microsoft.Testing.Platform" section covering package differences, test-project-as-executable in v3, VSTest vs MTP runner behavior, MSTest's parallel MTP-native story. **Downstream patch 2026-04-19 (skill v1.0.2, plugin v2.3.0):** FluentAssertions removed across every sample; RED-step alternative FA block deleted; Prove-It (`Assert.NotNull`), State-vs-Interaction (`Assert.Equal(col.OrderByDescending(...), col)`), DAMP (`Assert.Throws<E>` + `Assert.Contains`), TimeProvider / Arrange-Act-Assert (`Assert.True`) all rewritten to native `Xunit.Assert.X` and MSTest `Assert.X`. Test-pyramid diagram label changed `xUnit / MSTest + FluentAssertions` → `xUnit v3 / MSTest + native Assert`. Version Awareness "practical upshot" reframed — v3 is the current recommendation for new projects; v2 remains source-compatible and migrate when project capacity allows. Rationale: FluentAssertions v8 (Jan 2025) moved to the XCEED non-Apache license. |
| skills/using-agent-skills/SKILL.md | 4 | using-agent-skills | rewritten | `44dac80` | Meta skill retargeted at this marketplace: `dotnet-skills` overview, Claude Code activation paths (natural-language + `/dotnet-skills`), discovery tree uses our renamed skills (integration-testing-dotnet, frontend-ui-engineering-avalonia, performance-optimization-dotnet), Lifecycle Sequence covers all 19 non-meta skills, cross-references the companion avalonia-dev plugin; per-skill re-syncs continue for the other 20 skills but upstream-vs-downstream diffs on this file stop |

## Commands

Per-wrapper port status against upstream `.claude/commands/`. Ported in plugin version `2.1.0` (2026-04-19). All wrappers live at `plugins/dotnet-skills/commands/<name>.md` and retain a `Source & Modifications` footer matching the SKILL.md pattern.

| Upstream | Ported as | Status | Last sync SHA7 | Notes |
|----------|-----------|--------|----------------|-------|
| .claude/commands/spec.md | commands/spec.md | modified | `44dac80` | Skill reference retargeted to `dotnet-skills:`; clarifying questions reframed around .NET target framework / UI framework / data layer / test framework; spec output fields reframed for `dotnet` CLI and `.editorconfig` |
| .claude/commands/plan.md | commands/plan.md | modified | `44dac80` | Skill reference retargeted to `dotnet-skills:`; codebase-reading list names `*.sln` / `Directory.Packages.props` / `global.json` / `.csproj`; dependency-graph example names an EF Core vertical slice; verification steps use `dotnet build -warnaserror` / `dotnet test --filter` / `dotnet format --verify-no-changes` |
| .claude/commands/build.md | commands/build.md | modified | `44dac80` | Skill references retargeted; context-loading names `.editorconfig` / nullable mode / `MyApp.Contracts` / `MyApp.Core`; RED/GREEN step names xUnit/MSTest; build and test steps use `dotnet build -warnaserror` / `dotnet test` |
| .claude/commands/test.md | commands/test.md | modified | `44dac80` | **Critical substitution**: upstream references `agent-skills:browser-testing-with-devtools` which does not exist downstream — rewritten as `dotnet-skills:integration-testing-dotnet`, covering HTTP / DB / browser / Avalonia boundaries; framework labels changed from generic "tests" to "xUnit or MSTest tests"; commands use `dotnet test --filter FullyQualifiedName~...` for single-test runs |
| .claude/commands/review.md | commands/review.md | modified | `44dac80` | Skill references retargeted to `dotnet-skills:` and to the renamed skill `performance-optimization-dotnet`; each of the five axes gains .NET-specific checks (nullable annotations, `.editorconfig`/analyzers, DI lifetimes, `MyApp.Core`/`Infrastructure`/`Contracts` layering, FluentValidation, EF Core N+1, sync-over-async, `HttpClient` reuse); `file:line` references use `file.cs:line` format |
| .claude/commands/code-simplify.md | commands/code-simplify.md | modified | `44dac80` | Skill references retargeted; convention-sources list adds `.editorconfig` / analyzer severity / layering rules; simplification scan gains C#-idiomatic opportunities — `??`, `?.`, pattern-matching, `ArgumentNullException.ThrowIfNull`, async correctness, `record`/`record struct`; verification commands are `dotnet test`, `dotnet build -warnaserror`, `dotnet format --verify-no-changes` |
| .claude/commands/ship.md | commands/ship.md | modified | `1f66d57` | Skill reference retargeted; Code Quality swaps `console.logs` → stray `Console.WriteLine`/`Debug.WriteLine`/`Trace.WriteLine`; Security swaps `npm audit` → `dotnet list package --vulnerable --include-transitive` and adds `dotnet user-secrets` / Key Vault / antiforgery; Performance reframed around EF Core N+1, `IHttpClientFactory`, `AddOutputCache` — with Core Web Vitals made conditional on ASP.NET Core/Blazor context; Accessibility adds `AutomationProperties` for Avalonia/MAUI; Infrastructure names EF Core migrations, `IOptions<FeatureOptions>` / `Microsoft.FeatureManagement`, and Application Insights/OpenTelemetry; rollback step names `dotnet ef database update <Migration>` and the expand-contract pattern. **Upstream sync 2026-04-21 (plugin v2.4.0):** structurally rewritten from a linear pre-launch checklist into a three-phase **fan-out orchestrator** matching upstream `1f66d57` — Phase A fan-out spawns `code-reviewer` + `security-auditor` + `test-engineer` in parallel (single-turn Agent calls; .NET-grounded persona prompts covering nullable honesty / async correctness / DI lifetimes / EF Core N+1 for code-reviewer, OWASP→ASP.NET Core/`FromSqlRaw`/`dotnet list package --vulnerable` for security-auditor, xUnit/MSTest + `WebApplicationFactory` / Testcontainers / Playwright / `Avalonia.Headless.XUnit` boundaries for test-engineer); Phase B merge retains the pre-1f66d57 .NET checklist (Code Quality / Security / Performance cross-referenced with persona reports; Accessibility / Infrastructure / Documentation verified by the main agent directly); Phase C produces `## Ship Decision: GO | NO-GO` with blockers / acknowledged risks / rollback plan (`dotnet ef database update <PreviousMigration>` verified against a restored snapshot). Rule 5 skip-list extended to exclude changes that touch EF Core migrations or feature-flag gates. Link added to `../references/orchestration-patterns.md`. |

## Agents

Per-agent port status against upstream `agents/`. Ported in plugin version `2.2.0` (2026-04-19) — `2.2.0` failed manifest validation because an invalid `"agents"` key was added to `plugin.json`; `2.2.1` hotfixes that by removing the key and relying on Claude Code's auto-discovery of the `./agents/` directory. All personas live at `plugins/dotnet-skills/agents/<name>.md` and carry a `Source & Modifications` footer matching the SKILL.md pattern.

| Upstream | Ported as | Status | Last sync SHA7 | Notes |
|----------|-----------|--------|----------------|-------|
| agents/code-reviewer.md | agents/code-reviewer.md | modified | `1f66d57` | Persona reframed as a .NET/C# Staff Engineer with cross-refs to `code-review-and-quality`, `security-auditor` / `security-and-hardening`, and `performance-optimization-dotnet`; host-model lens note added at the top of the five-axis block; Correctness augmented with `default(T)`, sync-over-async, `CancellationToken` threading, honest nullable-RT annotations, and library `ConfigureAwait(false)` guidance for non-ASP.NET-Core hosts; Readability names `.editorconfig` as style authority; Architecture adds Minimal APIs vs controllers, `MyApp.Core`/`Infrastructure`/host layering, DI lifetime correctness, DTOs-in-`MyApp.Contracts`; Security adds FluentValidation, `dotnet user-secrets` + Key Vault, `FromSqlRaw` vs `FromSqlInterpolated`, Razor/Blazor `@Html.Raw`/`MarkupString` caution, `dotnet list package --vulnerable`; Performance adds EF Core N+1 / `Include` / `AsSplitQuery` / `AsNoTracking`, `Span<T>` / `ArrayPool<T>`, `IHttpClientFactory`, `async void`, Avalonia/Blazor UI-thread marshalling; finding references changed to `file.cs:line`; verification-story checklist uses `dotnet build -warnaserror` / `dotnet test` / FluentValidation / `dotnet list package --vulnerable`; Rule 7 adds `.editorconfig` + analyzer ruleset as style authority. **Upstream sync 2026-04-21 (plugin v2.4.0):** Composition block added (synced from upstream `1f66d57`) — "Invoke directly when / Invoke via / Do not invoke from another persona"; cross-links to `agents/README.md` and `references/orchestration-patterns.md`; names `/review` (single-persona) and `/ship` (parallel fan-out) as the standard entry points. |
| agents/security-auditor.md | agents/security-auditor.md | modified (heavy) | `1f66d57` | Full .NET rewrite of all five OWASP-aligned sections: **Input** → FluentValidation + DataAnnotations + `ModelState.IsValid`, `FromSqlRaw` vs `FromSqlInterpolated`, Razor/Blazor `@Html.Raw`/`MarkupString`, `RequestSizeLimit` + `FormOptions.MultipartBodyLengthLimit`, `Url.IsLocalUrl`, argument-array `Process.Start`, `BinaryFormatter` ban; **AuthN/AuthZ** → ASP.NET Core Identity + `PasswordHasher<TUser>`, `HttpOnly`/`Secure`/`SameSite` cookies, `[Authorize]` + policy-based `AddPolicy` + `RequireAuthorization()`, IDOR per-resource checks, `DataProtectorTokenProvider`, `[ValidateAntiForgeryToken]` + `AddAntiforgery`, `AddRateLimiter` + Identity lockout, JWT `ValidateIssuer`/`ValidateAudience`/`ValidateLifetime`/`ValidateIssuerSigningKey` + Key Vault key storage; **Data Protection** → `dotnet user-secrets` vs Key Vault, DTO separation, log PII scrubs, `UseHttpsRedirection` + `UseHsts`, `IDataProtectionProvider` key persistence (Azure Blob + Key Vault / DPAPI / Redis), GDPR/CCPA retention via ADRs, TDE / disk encryption; **Infrastructure** → security-header middleware (`NWebsec.AspNetCore.Middleware` or custom), `AddCors` restricted origins, `dotnet list package --vulnerable --include-transitive`, `UseDeveloperExceptionPage` guarded by `IsDevelopment`, least-privilege DB role, non-root container user; **Third-Party** → Key Vault for keys, HMAC webhook verification (Stripe / GitHub / Twilio), CDN `integrity` + `crossorigin`, OAuth Authorization Code + PKCE via `Microsoft.Identity.Web`, `IHttpClientFactory` + TLS 1.2+, bounded dead-letter. Severity table extended with .NET-specific examples; Rule 7 adds concrete .NET anti-patterns (no `ServerCertificateCustomValidationCallback = (_, _, _, _) => true`, no `[ValidateAntiForgeryToken]` removal, no `AllowAnyOrigin + AllowCredentials`). OWASP Top 10 + 5-section frame + severity taxonomy preserved. **Upstream sync 2026-04-21 (plugin v2.4.0):** Composition block added (synced from upstream `1f66d57`) — "Invoke directly when / Invoke via / Do not invoke from another persona"; cross-links to `agents/README.md` and `references/orchestration-patterns.md`; `/ship` named as the canonical parallel-fan-out entry point. |
| agents/test-engineer.md | agents/test-engineer.md | modified | `1f66d57` | Persona reframed as a .NET/C# QA Engineer with cross-refs to `test-driven-development` and `integration-testing-dotnet`; "Analyze Before Writing" augmented with xUnit v2/v3 vs MSTest detection, VSTest vs Microsoft.Testing.Platform runner choice, `Directory.Packages.props`/`global.json` reads; "Test at the Right Level" retargeted to the four .NET integration boundaries (unit + FluentAssertions, `WebApplicationFactory<Program>` HTTP, Testcontainers DB with explicit ban on `EntityFrameworkCore.InMemory`, `Microsoft.Playwright` browser E2E, `Avalonia.Headless.XUnit` desktop E2E); Prove-It step adds `dotnet test --filter FullyQualifiedName~<TestName>` and the "fails for the right reason" confirmation; `describe`/`it` JS example replaced with xUnit `[Fact]` + MSTest `[TestMethod]` + FluentAssertions + `CancellationToken`; naming convention `MethodUnderTest_Scenario_ExpectedBehavior`; scenario table adds Cancellation row (pre-cancelled token + mid-flight cleanup) and Time-dependent row (`TimeProvider` + `FakeTimeProvider`); new "Handle Time Correctly" section; Rules 5-6 forbid mocking `DbContext`/`IQueryable<T>` and forbid `Microsoft.EntityFrameworkCore.InMemory`; Rule 9 adds FluentAssertions guidance; Rule 10 names VSTest vs Microsoft.Testing.Platform runner choice. Analyze-before-writing / right-level / Prove-It / descriptive tests / scenarios / priority-tiers structure preserved. **Downstream patch 2026-04-19 (agent v1.0.1, plugin v2.3.0):** FluentAssertions removed from both samples and guidance — xUnit rewrite to native `Xunit.Assert.IsType<T>(result)` (uses the return value) + `Assert.Contains(key, dict.Keys)`; MSTest rewrite to `Assert.IsInstanceOfType<T>(result)` + `Assert.IsTrue(dict.ContainsKey(...))`. Rule 9 flipped from "use FluentAssertions when the project does" to "do not introduce FluentAssertions — v8+ is non-Apache-licensed". Frontmatter description updated to "xUnit v3 (or v2) or MSTest with native `Assert.X`". Analyze-Before-Writing treats encountered FluentAssertions as a migration signal to flag, not a neutral style detection. **Upstream sync 2026-04-21 (plugin v2.4.0):** Composition block added (synced from upstream `1f66d57`) — "Invoke directly when / Invoke via / Do not invoke from another persona"; cross-links to `agents/README.md` and `references/orchestration-patterns.md`; `/test` (TDD workflow) and `/ship` (parallel fan-out) named as the standard entry points. |

## Copilot CLI forms (downstream-only)

Copilot CLI–shaped `.agent.md` wrappers for the three ported personas. Added in plugin version `2.5.0` for GitHub Copilot CLI compatibility — these files have **no upstream counterpart**, so the upstream-sync ledger columns don't apply. The wrappers pair with the canonical Claude Code `.md` forms in the Agents table above; body content is verbatim between the pair.

**Drift discipline.** Bodies must stay in lockstep. When editing a persona, update both `<name>.md` and `<name>.agent.md`. Upstream attribution is not duplicated — the `.agent.md` footer points to the `.md` sibling as canonical for upstream pin, changelog, and license. This means an upstream re-sync only touches the `.md` files; the `.agent.md` wrappers are re-synced from their `.md` siblings by hand.

| Copilot form | Canonical sibling | Form status | Added in | Notes |
|--------------|-------------------|-------------|----------|-------|
| agents/code-reviewer.agent.md | agents/code-reviewer.md | in-sync | `2.5.0` | Frontmatter: `name`, `description` only (Copilot CLI minimum). Body verbatim from sibling except the Claude `source:` key is omitted. Composition block keeps the README / orchestration-patterns cross-links; the Claude-Code-specific "subagents cannot spawn other subagents" platform note was trimmed because Copilot CLI doesn't have that platform guarantee. Source & Modifications footer converted to a Copilot-form variant that points to the canonical sibling. |
| agents/security-auditor.agent.md | agents/security-auditor.md | in-sync | `2.5.0` | Same shape as above. |
| agents/test-engineer.agent.md | agents/test-engineer.md | in-sync | `2.5.0` | Same shape as above. Note the sibling's v2.3.0 FluentAssertions-removal patch applies to both forms — any future FluentAssertions-related edit must land in both files. |

**What's not mirrored to Copilot forms (and why).**

- **8 `commands/*.md`** — Copilot CLI has no custom-slash-command surface as of 2026-04 ([github/copilot-cli#618](https://github.com/github/copilot-cli/issues/618) open, unimplemented). `.github/prompts/*.prompt.md` is read only by VS Code Copilot, and only from a consumer repo's root — not from inside an installed plugin. Contributors working via Copilot CLI use the three `/agent <name>` entry points and rely on skill auto-activation for the lifecycle workflows.
- **21 `skills/<name>/SKILL.md`** — already portable. `SKILL.md` with `name` + `description` frontmatter is a cross-vendor standard; Copilot CLI reads them directly. No second form needed.

## Non-skill upstream directories

| Upstream | Disposition | Notes |
|----------|-------------|-------|
| agents/ | **ported** (v2.2.0, hotfix v2.2.1; docs expanded v2.4.0; README relocated v2.5.1) | All 3 personas ported; see the Agents table above. `agents/README.md` ported at `1f66d57` in v2.4.0, then relocated to `references/agents-overview.md` in v2.5.1 to stop Copilot CLI's agent loader from scanning it as a malformed agent ([github/copilot-cli#2187](https://github.com/github/copilot-cli/issues/2187)). See the detailed ledger row below. |
| agents/README.md | **ported + relocated** (v2.4.0, moved v2.5.1) | See detailed row below (Ported as: `references/agents-overview.md`) |
| references/ | **partially ported** (v2.4.0) | `orchestration-patterns.md` promoted to `plugins/dotnet-skills/references/orchestration-patterns.md` in v2.4.0 (see row below). Remaining reference files (testing/performance/security/accessibility checklists) are still vendor-only — promote per-skill as ports land. |
| references/orchestration-patterns.md | **ported** (v2.4.0) | See new row below |
| hooks/ | vendor-only | Re-evaluate when Wave 3 lands |
| docs/ | vendor-only | Reference only |
| .claude/commands/ | **ported** (v2.1.0, /ship restructured v2.4.0) | All 7 wrappers ported; see the Commands table above |
| AGENTS.md, CLAUDE.md, CONTRIBUTING.md | vendor-only | Not intended for downstream replication. Note: upstream `AGENTS.md` grew substantially at `1f66d57` with an "Orchestration: Personas, Skills, and Commands" section — that guidance is now mirrored in our ported `references/agents-overview.md` (relocated from `agents/README.md` in v2.5.1) + `references/orchestration-patterns.md`, so the underlying information is available to downstream users without replicating the vendor-only file. |

## Agent docs & references

Per-file port status for standalone agent documentation and cross-persona references that aren't individual personas or per-skill reference files.

| Upstream | Ported as | Status | Last sync SHA7 | Notes |
|----------|-----------|--------|----------------|-------|
| agents/README.md | references/agents-overview.md | modified | `1f66d57` | Persona-directory README with decision matrix, worked examples for valid and invalid orchestration, persona rules, and Claude Code interop note. Persona descriptions retargeted to `.NET Staff Engineer` / `.NET Security Engineer` / `.NET QA Engineer`; skill references prefixed with `dotnet-skills:`; direct-invocation examples use C# file paths (`AuthorizationHandler.cs`, `CheckoutEndpoints.cs`); `/ship` fan-out diagram annotated with the .NET merge checklist (`dotnet test`, `dotnet build -warnaserror`, `dotnet list package --vulnerable`, `dotnet ef migrations list`) and rollback step (`dotnet ef database update <PreviousMigration>`); competing-hypothesis debugging example names a flaky `TaskCanceledException` scenario instead of the generic upstream checkout-hang; "Adding a new persona" step 6 added — carry the "Source & Modifications" footer on ported personas for `scripts/sync-agent-skills.ps1 -Verify` drift tracking; upstream `AGENTS.md` link replaced with `CLAUDE.md` (vendor-only file in this repo). Core structure preserved from upstream. **Downstream patch 2026-04-22 (plugin v2.5.1):** Relocated from `plugins/dotnet-skills/agents/README.md` to `plugins/dotnet-skills/references/agents-overview.md`. Copilot CLI's agent loader scans every `.md` file in `agents/` (not just `*.agent.md`, despite what its docs say) and requires valid agent frontmatter on each one — a frontmatter-less README produced install-time warnings (`custom agent markdown frontmatter is malformed`). Issue [github/copilot-cli#2187](https://github.com/github/copilot-cli/issues/2187) requests an exclusion mechanism but remains open. Moving the file matches the de-facto convention in [github/awesome-copilot](https://github.com/github/awesome-copilot) (no non-agent `.md` in `agents/`). All 6 persona files (`code-reviewer.md`, `code-reviewer.agent.md`, `security-auditor.md`, `security-auditor.agent.md`, `test-engineer.md`, `test-engineer.agent.md`) and `commands/dotnet-skills.md` had their cross-links rebased from `README.md` / `../agents/README.md` to `../references/agents-overview.md` / `references/agents-overview.md`. The moved file's own internal links (persona table cells, pattern-catalog cross-links, intro paragraph about auto-discovery) rebased to reflect the new location. |
| references/orchestration-patterns.md | references/orchestration-patterns.md | modified | `1f66d57` | Pattern catalog (direct invocation / single-persona slash command / parallel fan-out with merge / sequential user-driven pipeline / research isolation) + Claude Code compatibility + Agent Teams worked example + 4 anti-patterns + decision flow + catalog-addition criteria. Intro grounds the catalog in this plugin's actual personas and commands; direct-invocation examples use C# file paths; Pattern 2 names skill references with `dotnet-skills:` prefix; Pattern 3 merge-step description specifies the .NET pre-launch checklist and `dotnet ef database update <PreviousMigration>` rollback; Pattern 4 cross-references `plugins/dotnet-skills/skills/using-agent-skills/SKILL.md`; Pattern 5 examples use .NET investigation scenarios (`FromSqlRaw` call sites, EF Core provider ADRs, `[Obsolete]`-marked APIs in `MyApp.Core`); "Where personas live" notes the 2.2.0 manifest-validation regression (adding an `agents` key to `plugin.json` fails install) — a downstream-specific caveat from this marketplace's history; competing-hypothesis worked example re-grounded in a `TaskCanceledException` scenario with Application Insights / `dotnet-counters` signals, hypothesis list names `Task.WhenAll` / policy-based `[Authorize]` + Key Vault sync-over-async / EF Core query plan regression / Stripe webhook retries, test-engineer mitigation uses `WebApplicationFactory<Program>` + `DelegatingHandler`; cost-expectation names an EF Core partial-rollout rollback; anti-pattern B names `[Authorize]` attributes; anti-pattern C names EF Core migration ordering as a hand-off failure mode. Core structure (5 endorsed patterns, Claude Code compatibility section, Agent Teams worked example, 4 anti-patterns, decision flow, catalog-addition criteria) preserved from upstream. |
